---
dg-publish: true
Teacher: 吕卫国
commit: true
date: 2026-03-02
---

> [!NOTE] 学习要点

### （一）受精及受精卵发育、输送与着床
1. 受精卵形成
2. 受精卵着床
	- 30h-卵裂；3日-桑葚胚；4日-进入宫腔、形成早期胚囊；5-6日-晚期胚囊、着床
	- ==着床==：晚期胚囊植入子宫内膜的过程。*定位、粘附、穿透* 3个过程
	- 必备条件：透明带消失、合体滋养细胞形成、胚囊和子宫内膜发育同步且功能协调、孕妇体内有足够的孕酮
### （二）胚胎、胎儿发育特征及胎儿生理特点
【循环系统】
	- 与胎盘相连
	- 下腔静脉右心房卵园孔左心房左心室大循环
	- 脐静脉内为动脉血，脐动脉内为静脉血
	- 肺循环压力较高
	- 肝、心、头、上肢血液的氧含量较高
【血液系统】早孕期间，红细胞来源卵黄囊、肝脏出生时胎儿血红蛋白占25%
【呼吸系统】孕16周 出现呼吸运动，羊水进出呼吸道
【消化系统】孕16周胃肠功能基本建立；胆红素胆绿素墨绿色胎粪
【泌尿系统】孕14周基本建立，参与羊水循环
### （三）胎儿附属物的形成与功能
- 胎儿附属物：胎盘、胎膜、脐带、羊水
- **胎盘**：底蜕膜+叶状绒毛膜+羊膜
	- 功能：维持胎儿在宫内营养、发育的重要器官
	- 代谢功能：气体交换、营养物质供应和废物排出
	- 防御功能：胎盘屏障、母体抗体
	- 内分泌功能：HCG HPL 妊娠特异性蛋白 甾体激素 缩宫素酶等
	- 免疫功能：妊娠耐受
### （四）妊娠期母体的变化

【子宫】肌细胞*肥大*（约20倍），少量肌细胞增生
	- 不同程度右旋
	- 子宫内膜在孕酮的作用下演变为*蜕膜*
	- 无痛性不规则宫缩(Braxton Hicks)，不扩张宫颈
	- 血流量增加，但宫缩时减少
	- *峡部* 变软、拉长(7－10cm) 形成子宫下段
	- *宫颈* 充血、变软、水肿、肥大
【卵巢】早孕期间，分泌甾体激素，维持妊娠；有时可形成黄素囊肿，卵泡膜细胞黄素囊肿，卵泡膜细胞黄素化所致
【阴道】肌层肥厚、变软、为软产道的一部分
【外阴及子宫支持组织】变软、伸展性增加
【循环系统】
	- 心脏：心肌肥厚，心排出量增加
	- 血压：早中期偏低，晚期略高但脉压差增大。仰卧位低血压综合症
	- 血液系统：血容量增加，32－34周高峰；血液相对稀释；血红蛋白下降、粒细胞增加；大部分凝血因子增加（高凝状态）
【呼吸系统】肺活量无变化，潮气量、每分钟通气量增加；呼吸道粘膜充血水肿，抵抗力下降
【泌尿系统】肾血浆流量和肾小球滤过率增加→尿量增加；妊娠生理性尿糖（注意与糖尿病鉴别）
【输尿管】增粗、延长、屈曲→尿流缓慢→肾盂积水
【膀胱】容量减少排尿次数增加
【内分泌系统】
	- 垂体：细胞肥大，若缺血坏死→Sheehan综合征；分泌 FSH、LH减少，PRL增高
	- 甲状腺：甲状腺素增高，游离T3,T4不高；基础代谢率增高
	- 胰腺：胰岛素分泌增加
	- 皮肤：妊娠斑、妊娠纹
	- 骨骼、关节、韧带：松弛、伸展








---
```dataviewjs
const cur = dv.current();
const filePath = cur?.file?.path ?? null;

function extractPath(item){
    if(!item) return null;
    if(typeof item === "string") return item;
    if(item.path) return item.path;
    if(item.file && item.file.path) return item.file.path;
    return null;
}
function isValidNote(path){
    if(!path) return false;
    const lower = path.toLowerCase();
    return !lower.match(/\.(png|jpg|jpeg|gif|svg|webp|mp4|mov|zip|json|csv|xlsx|docx)$/);
}
function displayNameFor(path){
    const pg = dv.page(path);
    return pg?.file?.name ?? path.split('/').pop();
}

let outRaw = [];
try {
    if(typeof dv.outgoing === "function" && filePath) outRaw = dv.outgoing(filePath);
    else if (cur?.outlinks) outRaw = cur.outlinks;
    else if (cur?.file?.outlinks) outRaw = cur.file.outlinks;
} catch(e) { outRaw = []; }
const outPaths = [...new Set((outRaw || []).map(extractPath).filter(isValidNote))];

let inRaw = [];
try {
    if(typeof dv.incoming === "function" && filePath) inRaw = dv.incoming(filePath);
    else {
        inRaw = dv.pages().where(p => {
            try {
                const ol = p.file?.outlinks ?? p.outlinks ?? [];
                return ol.some(l => extractPath(l) === filePath);
            } catch(e){ return false; }
        }).array();
    }
} catch(e) { inRaw = []; }
const inPaths = [...new Set((inRaw || []).map(extractPath).filter(isValidNote))];

const container = document.createElement("div");
container.className = "link-dashboard";

function createCard(title, paths, emptyMsg, icon){
    const card = document.createElement("div");
    card.className = "link-card";

    const h3 = document.createElement("h3");
    h3.textContent = `${icon} ${title} (${paths.length})`;
    card.appendChild(h3);

    if(paths.length){
        // 排序
        paths.sort((a,b) => (displayNameFor(a) ?? a).localeCompare(displayNameFor(b) ?? b));
        const ol = document.createElement("ol");
        for(const p of paths){
            const li = document.createElement("li");

            const a = document.createElement("a");
            a.href = "#";
            a.textContent = displayNameFor(p);
            a.style.cursor = "pointer";

            // 点击通过 Obsidian API 打开链接（在新 leaf）
            a.addEventListener("click", (e) => {
                e.preventDefault();
                try {
                    app.workspace.openLinkText(p, "", true);
                } catch(err) {
                    // 回退尝试按显示名打开
                    try { app.workspace.openLinkText(a.textContent, "", true); } catch(e2){ console.error(e2); }
                }
            });

            li.appendChild(a);
            ol.appendChild(li);
        }
        card.appendChild(ol);
    } else {
        const p = document.createElement("p");
        p.textContent = emptyMsg;
        p.style.opacity = "0.75";
        card.appendChild(p);
    }

    return card;
}

container.appendChild(createCard("本文引用的笔记", outPaths, "📭 本文未引用其他笔记。", "📓"));
container.appendChild(createCard("引用了本文的笔记", inPaths, "📪 目前没有笔记引用本文。", "📑"));

dv.container.appendChild(container);


```

